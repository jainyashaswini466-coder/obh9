
Problem 6: Find Maximum Element
 
 function findMax(arr) {
    // Use linear search approach to find maximum
    
}

const scores = [45, 89, 23, 97, 56];
console.log(findMax(scores)); // 97

//-------------------------------------------------------------

Problem 7: Count Occurrences

function countOccurrences(arr, target) {
    // Count how many times a target appears in array
}

const grades = ['A', 'B', 'A', 'C', 'A', 'B'];
console.log(countOccurrences(grades, 'A')); // 3

//-------------------------------------------------------------

Problem 8: Find All Products Above Price

const products = [
    { name: 'Laptop', price: 45000 },
    { name: 'Mouse', price: 500 },
    { name: 'Keyboard', price: 1500 }
];

function findExpensiveProducts(products, minPrice) {
    // Return all products with price greater than given value
}

console.log(findExpensiveProducts(products, 1000)); 
// [{ name: 'Laptop', price: 45000 }, { name: 'Keyboard', price: 1500 }]

//-------------------------------------------------------------

